export const requiredMsg = 'Sorry, but it`s field is required';
export const shortMsg = 'It`s too short';
export const passwordMatchMsg = 'Passwords must match ya fool';
export const passwordLowerMsg = 'The string must contain at least 1 lowercase alphabetical character';
export const passwordUpperMsg = 'The string must contain at least 1 uppercase alphabetical character';
export const passwordNumericMsg = 'The string must contain at least 1 numeric character';
